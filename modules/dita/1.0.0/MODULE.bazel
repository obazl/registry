module(
    name = "dita",
    version = "1.0.0",
    compatibility_level = 1,
    bazel_compatibility = [">=6.4.0"]
)

## bcr deps:
bazel_dep(name = "platforms",      version = "0.0.8")
bazel_dep(name = "bazel_skylib",   version = "1.4.2")

# dita = use_extension(
#     "@dita//extensions:dita.bzl", "dita"
# )

# dita.config()
# use_repo(dita, "dita__")

